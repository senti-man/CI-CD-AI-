# 팀 온보딩 가이드 — Docker/Git 처음 써보는 사람용

이 프로젝트는 Docker, Git, 터미널을 다루게 되는데, 팀원 전부가 이런 도구에 익숙한 건 아닐 거예요. 이 문서는 "이게 뭔지도 잘 모르겠다"는 상태에서 시작해도 따라올 수 있게 쓴 겁니다. 이미 익숙하신 분은 건너뛰셔도 됩니다.

---

## 1. 이 프로젝트에서 쓰는 도구가 각각 뭔지

| 도구 | 한 줄 설명 |
|---|---|
| **터미널 (PowerShell)** | 마우스로 클릭하는 대신 글자로 명령을 입력해서 컴퓨터를 조작하는 화면. Windows에는 기본으로 깔려있습니다. |
| **Git** | 코드가 바뀐 이력을 기록하고 관리하는 도구. "누가 언제 뭘 바꿨는지"를 전부 저장합니다. |
| **GitHub / Gitea** | Git으로 관리하는 코드를 인터넷(또는 우리끼리만 쓰는 로컬 서버)에 올려두고 여러 명이 같이 보고 수정할 수 있게 해주는 서비스. GitHub는 전 세계가 쓰는 공개 서비스고, Gitea는 우리가 직접 우리 컴퓨터에 설치해서 쓰는 "우리만의 미니 GitHub"입니다.
| **Docker** | 프로그램을 실행하는 데 필요한 환경(운영체제, 라이브러리 등)을 통째로 "상자(컨테이너)"에 담아서, 어느 컴퓨터에서든 똑같이 실행되게 해주는 도구. "내 컴퓨터에서는 되는데 왜 안 되지"라는 문제를 없애줍니다. |
| **CI/CD, Actions/워크플로우** | 코드가 바뀔 때마다 "테스트 자동 실행", "배포 자동 실행" 같은 걸 사람이 손으로 안 하고 자동으로 돌아가게 만든 시스템. 이번 프로젝트의 공격 대상이 바로 이겁니다. |
| **PR (Pull Request)** | "이렇게 코드를 바꾸고 싶은데, 확인하고 반영해주실래요?" 라고 요청하는 것. 팀원(또는 프로젝트 관리자)이 검토 후 승인하면 실제 코드에 반영됩니다. |
| **토큰 (Access Token)** | 비밀번호 대신 쓰는 긴 임의의 문자열. 프로그램끼리 자동으로 인증할 때 비밀번호보다 안전하고 편해서 이걸 씁니다. |

---

## 2. 시작 전 체크리스트

1. Docker Desktop 설치돼 있고 실행 중인지 (`docker --version`으로 확인)
2. Git 설치돼 있는지 (`git --version`으로 확인)
3. 터미널(PowerShell)을 열 줄 아는지 — 시작 메뉴에서 "PowerShell" 검색
4. 코드 에디터(메모장도 되지만, VS Code 추천 — 무료, 설치만 하면 됨)

---

## 3. 터미널 최소 개념

터미널은 "지금 내가 어느 폴더에 있는지"가 항상 정해져 있고, 명령은 그 폴더를 기준으로 동작합니다.

- `cd 폴더경로` — 그 폴더로 이동 (Change Directory)
- 지금 위치 확인은 프롬프트에 항상 경로가 표시됩니다 (예: `PS C:\Users\Admin\Downloads>`)
- 명령을 잘못 입력하면 대부분 빨간 글씨로 에러가 뜹니다 — **에러 메시지를 무서워하지 말고 그대로 복사해서 팀원이나 AI에게 보여주세요.** 에러 메시지 자체가 원인을 알려주는 경우가 대부분입니다.

---

## 4. 오늘 실제로 겪은 에러들 (FAQ)

전부 이 프로젝트를 실제로 진행하면서 만난 문제들입니다. 팀원들도 똑같은 걸 겪을 가능성이 높아서 미리 정리해둡니다.

### Q1. PowerShell에서 명령을 여러 줄로 나눠 썼는데 각 줄이 따로따로 에러가 나요
**원인**: 줄 끝의 `\`는 Mac/Linux(bash)에서만 "다음 줄과 이어서 실행"이라는 뜻입니다. Windows PowerShell은 이걸 그냥 무시하고 그 줄에서 명령이 끝났다고 봅니다.
**해결**: 명령을 한 줄로 길게 이어서 쓰거나, PowerShell에서는 줄 끝에 백틱(`` ` ``)을 씁니다 (단, 백틱 뒤에 공백이 하나라도 남으면 또 깨지니 한 줄로 쓰는 게 제일 안전합니다).

### Q2. Git으로 push할 때 "Failed to authenticate user" 에러가 나요
**원인**: Windows의 Git 자격증명 관리자(GCM)가 로컬 서버(Gitea)를 GitHub 같은 걸로 착각하고 헤매다가 인증에 실패합니다.
**해결**: 비밀번호 대신 **개인 액세스 토큰(Personal Access Token)** 을 만들어서, `git remote set-url origin 'http://아이디:토큰@주소/저장소.git'` 형태로 아예 URL에 인증 정보를 박아버리면 GCM을 거치지 않아서 안전하게 됩니다. (작은따옴표로 전체를 감싸는 걸 잊지 마세요 — 토큰에 특수문자가 있으면 따옴표 없이는 깨집니다.)

### Q3. 비밀번호/토큰에 특수문자가 있으면 명령이 이상하게 깨져요
**원인**: PowerShell은 `#`을 주석 시작 기호로, `&`, `|` 같은 문자도 특별하게 해석합니다. 따옴표 없이 그대로 붙여넣으면 뒷부분이 통째로 날아가거나 엉뚱하게 해석됩니다.
**해결**: 특수문자가 들어간 값은 항상 작은따옴표(`'...'`)로 감싸세요. 가능하면 영문/숫자로만 된 토큰을 쓰는 게 제일 안전합니다.

### Q4. 파일을 만들었는데(`generate-config > 파일명` 같은 식으로) 프로그램이 그 파일을 못 읽어요
**원인**: PowerShell의 `>` 리다이렉트는 기본적으로 파일을 UTF-16이라는 형식으로 저장하는데, 많은 프로그램(특히 Docker 컨테이너 안의 프로그램)은 UTF-8 형식만 읽을 수 있습니다.
**해결**: 중요한 설정 파일은 텍스트 에디터로 직접 열어서 내용을 확인하고, 이상하면 새로 만드는 게 제일 확실합니다.

### Q5. 워크플로우(자동화 스크립트)를 수정했는데 아무 에러 없이 그냥 조용히 실행이 안 돼요
**원인**: YAML이라는 설정 파일 형식은 들여쓰기(공백 개수)가 정확히 맞아야 합니다. 다른 줄과 들여쓰기 칸 수가 다르면 파일 전체가 "잘못된 형식"으로 인식되어 아예 실행 목록에서 빠져버리는데, 보통 에러 메시지도 안 뜨고 그냥 안 보입니다.
**해결**: 코드를 복사해 붙여넣을 때 들여쓰기가 그대로 유지됐는지 꼭 확인하세요. 웹 화면(Gitea/GitHub)에서 직접 수정하는 게 로컬 에디터보다 이런 실수가 적습니다.

### Q6. `pip install`이 "externally-managed-environment" 에러로 실패해요
**원인**: 최신 리눅스 배포판은 시스템 전체에 영향을 주는 파이썬 패키지 설치를 기본적으로 막아둡니다.
**해결**: 일회용 컨테이너 안에서는 `pip install --break-system-packages 패키지명`으로 이 제한을 건너뛰어도 안전합니다.

---

## 5. 막혔을 때 팀에 도움 요청하는 법

1. **에러 메시지 전체를 그대로 복사**하세요 (일부만 캡처하면 원인을 못 찾습니다).
2. 방금 **무슨 명령을 실행했는지**도 같이 보내주세요.
3. "안 돼요"보다 "이 명령을 쳤더니 이런 에러가 떴어요"가 훨씬 빨리 해결됩니다.

이 프로젝트의 `cicd_supply_chain_demo/HANDS_ON_GUIDE.md`에 실제 단계별 실습이 정리돼 있으니, 여기서 기초를 익힌 다음 그 문서를 따라가시면 됩니다. 프로젝트 파일을 GitHub에 올려서 팀과 공유하는 방법은 `GITHUB_UPLOAD_GUIDE.md`를 참고하세요.

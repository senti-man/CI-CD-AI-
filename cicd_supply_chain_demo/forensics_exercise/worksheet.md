# 분석 워크시트

이름/팀:
날짜:

---

## Part 1 — 초동 조사 (`evidence/phase1_initial_investigation/` 만 사용)

**Q1.** `git_log.txt`를 보세요. 3개 커밋 중 어떤 게 가장 의심스럽습니까?
누가(작성자), 언제 만들었는지 적고, 왜 의심스러운지 이유를 쓰세요.

```
답:
```

**Q2.** `suspicious_commit.diff`를 정적 분석하세요.
- 추가된 스텝의 이름은 무엇입니까? 그 이름은 실제로 하는 일과 일치합니까?
- 이 스텝이 참조하는 시크릿 이름은 무엇입니까?
- 이 스텝이 데이터를 보내는 목적지(URL)는 어디입니까?
- 종합적으로, 이건 정상적인 CI 최적화입니까 악성 코드입니까? 근거를 3가지 이상 쓰세요.

```
답:
```

**Q3.** `pr1_metadata.json`을 여세요.
- 이 PR을 만든(연) 계정(`user.login`)은 누구입니까?
- 이 PR을 병합한 계정(`merged_by` 또는 관련 필드)은 누구입니까?
- 두 계정이 다르다는 사실이 이 사고에서 왜 중요합니까?

```
답:
```

**Q4. 타임라인을 다시 짜보세요.** `actions_runs_1_2.json`과
`attacker_server_log_excerpt.txt`의 타임스탬프를 가지고 아래 표를 채우세요.

| 시각 (UTC) | 사건 |
|---|---|
|  | PR #1이 main에 병합되어 워크플로우가 트리거됨 |
|  | 공격자 서버가 시크릿 값을 수신함 |

병합부터 유출까지 몇 초가 걸렸습니까?

```
답:
```

**Q5. IoC(침해지표)를 최소 4개 뽑으세요.** (네트워크/파일/계정/행위 등 카테고리를
섞어서 뽑아보세요)

```
1.
2.
3.
4.
```

**Q6. MITRE ATT&CK 매핑.** 이 공격의 각 단계에 해당하는 Technique를 최소
3개 찾아 ID와 이름을 적으세요. (초기 침투 / 실행 / 수집 / C2 / 유출 중에서 고르기)

```
1.
2.
3.
```

---

## Part 2 — 전체 그림 (`evidence/phase2_full_incident_record/` 까지 확인)

**Q7.** `git_log_full.txt`를 보고, 유지관리자(`demoadmin`)가 사고 발견 후
취한 조치를 시간 순으로 정리하세요 (최소 4단계).

```
답:
```

**Q8.** `attacker_server_log_full.txt`를 끝까지 읽어보면 `11:11` 이후에
`raw body: secret=` (값이 비어있음)인 요청들이 여러 번 등장합니다.
- 이 요청들은 어느 워크플로우/어느 이벤트(`push` vs `pull_request`)에서
  발생한 것으로 추정됩니까? (`actions_runs_all.json`, `pull_requests_all.json`
  참고)
- 왜 시크릿 값이 비어있을까요? 우연일까요, 아니면 플랫폼이 일부러 막은
  걸까요? 자기 나름의 가설을 세워보고, 이게 왜 중요한 보안 설계인지도
  써보세요.

```
답:
```

**Q9.** `branch_protection_config.json`을 보세요.
- `status_check_contexts`에 어떤 체크가 필수로 지정되어 있습니까?
- 이 설정은 어떤 유형의 공격을 막을 수 있습니까?
- 이 설정만으로는 막을 수 *없는* 공격 경로가 있다면 무엇일까요? (힌트: Q8과
  연결지어 생각해보세요 — "병합 전"과 "병합 후"의 차이)

```
답:
```

**Q10.** `pull_requests_all.json`에서 PR #2와 #3을 비교하세요.
- 두 PR의 상태(state)와 브랜치가 어떻게 다릅니까?
- `attacker01`이 PR #2를 닫고 #3을 새로 연 이유를 추론해보세요. (힌트: PR을
  다시 열면 Gitea가 체크를 다시 평가하는 조건이 무엇이었는지 떠올려보세요)

```
답:
```

---

## Part 3 — 사고 보고서 초안

아래 형식으로 실제 팀 프로젝트 보고서에 쓸 초안을 작성하세요.

### 1. 사고 원인

```

```

### 2. 영향 범위

```

```

### 3. 탐지 방법 (사후적으로 어떻게 알아차렸는가 / 어떻게 하면 더 빨리 알아챌 수 있었는가)

```

```

### 4. 대응 조치 (실제로 취해진 것 + 여러분이라면 추가로 했을 것)

```

```

### 5. 재발 방지 대책 (최소 3가지, 구체적으로)

```
1.
2.
3.
```
